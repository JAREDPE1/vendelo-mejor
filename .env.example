# Set the real public origin before deploying (no trailing path).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Complete with an actual monitored address before public launch.
CONTACT_EMAIL=
# Local preview only. No advertisement scripts are included.
NEXT_PUBLIC_SHOW_AD_PLACEHOLDERS=false
